import { createContext } from "react";

const AuthContex= createContext(null);
const auth= getAuth

const AuthProvider = () => {
  return (
    <div>
      
    </div>
  );
};

export default AuthProvider;